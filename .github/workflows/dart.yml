name: Build APKs and Email

on:
  pull_request:
    branches:
      - production

jobs:
  build-apk:
    name: Build Debug and Release APKs
    runs-on: ubuntu-latest

    steps:
      # Checkout the code
      - name: Checkout Code
        uses: actions/checkout@v3

      # Set up Java
      - name: Set up Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '18'

      # Set up Flutter
      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.19.6'

      # Restore Keystore
      - name: Decode Keystore File
        run: |
          echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > android/app/keystore.jks

      # Install Dependencies
      - name: Install Dependencies
        run: flutter pub get

      # Build Debug APK
      - name: Build Debug APK
        run: flutter build apk --debug

      # Build Release APK
      - name: Build Release APK
        run: |
          flutter build apk --release \
          --dart-define=KEYSTORE_PATH=android/app/keystore.jks \
          --dart-define=KEYSTORE_ALIAS=${{ secrets.ANDROID_KEY_ALIAS }} \
          --dart-define=KEYSTORE_PASSWORD=${{ secrets.ANDROID_KEYSTORE_PASSWORD }} \
          --dart-define=KEY_PASSWORD=${{ secrets.ANDROID_KEY_PASSWORD }}

      # Rename APK Files
      - name: Rename APK Files
        run: |
          PR_TITLE="${{ github.event.pull_request.title }}"
          APK_NAME=${{ github.repository }}
          if [ -z "$PR_TITLE" ]; then
            PR_TITLE=$APK_NAME
          fi
          mv build/app/outputs/flutter-apk/app-debug.apk build/app/outputs/flutter-apk/${PR_TITLE}_debug.apk
          mv build/app/outputs/flutter-apk/app-release.apk build/app/outputs/flutter-apk/${PR_TITLE}_release.apk

      # Upload APKs as Artifacts
      - name: Upload Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: Debug APK
          path: build/app/outputs/flutter-apk/*_debug.apk

      - name: Upload Release APK
        uses: actions/upload-artifact@v4
        with:
          name: Release APK
          path: build/app/outputs/flutter-apk/*_release.apk

      # Retrieve Artifact URLs
      - name: Get Artifact Download URLs
        id: get_urls
        run: |
          # Fetch artifact details using GitHub API
          artifacts=$(curl -s -H "Authorization: Bearer ${{ secrets.BEARER_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts")
          
          # Extract artifact IDs for Debug and Release APKs
          debug_artifact_id=$(echo "$artifacts" | jq -r '.artifacts[] | select(.name == "Debug APK") | .id')
          release_artifact_id=$(echo "$artifacts" | jq -r '.artifacts[] | select(.name == "Release APK") | .id')
          
          # Construct download URLs using artifact IDs
          debug_apk_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts/$debug_artifact_id"
          release_apk_url="https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts/$release_artifact_id"
      
          # Save output variables to environment files
          echo "DEBUG_APK_URL=$debug_apk_url" >> $GITHUB_ENV
          echo "RELEASE_APK_URL=$release_apk_url" >> $GITHUB_ENV


      # Email APKs
      - name: Send Email
        uses: dawidd6/action-send-mail@v3
        with:
          server_address: dcodax.net
          server_port: 465
          username: ${{ secrets.EMAIL_USERNAME }}
          password: ${{ secrets.EMAIL_PASSWORD }}
          from: ${{ secrets.EMAIL_USERNAME }} # Specify the sender email
          subject: "New APKs Generated - ${{ github.event.pull_request.title }}"
          body: |
            The debug and release APKs have been generated. You can download them from the following links:
            
            - Debug APK: $DEBUG_APK_URL
            - Release APK: $RELEASE_APK_URL
          to: "shafiquecbl@gmail.com,usamadcodax@gmail.com" # Add multiple recipients here
